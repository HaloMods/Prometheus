using System;

namespace Core.Project
{
  /// <summary>
  /// Represents the three version designations: alpha, beta, and release.
  /// </summary>
  public enum VersionDesignation
  {
    Alpha,
    Beta,
    Release
  }
}
