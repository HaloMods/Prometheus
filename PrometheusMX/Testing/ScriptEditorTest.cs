using Prometheus.Controls.ScriptEditor;
using Prometheus.Dialogs;

namespace Prometheus.Testing
{
#if DEBUG
  /*[TestInfo("rec0", "Script Editor", "Open's a script editor for Halo PC.")]
  public class ScriptEditorTest : ITest
  {
    public void PerformTest()
    {
      PrometheusGUI gui = (PrometheusGUI) PrometheusGUI.ActiveForm;
      ScriptEditorControl editor = new ScriptEditorControl();
      editor.Create(Core.Prometheus.Instance.GetGameDefinitionByGameID("halo1pc"));
      gui.AddControlToFileTabs(editor);
    }
  }*/
#endif
}
